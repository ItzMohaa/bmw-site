import { motion } from "framer-motion";

const Technology = () => {
    return ( <motion.div initial={{opacity:0}} animate={{opacity:1}} transition={{duration:1}} className="technology">
       
    </motion.div> );
}
 
export default Technology;